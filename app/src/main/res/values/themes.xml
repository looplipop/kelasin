<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme: no ActionBar, edge-to-edge transparent system bars -->
    <style name="Theme.Kelasin" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Truly transparent status bar + nav bar — Compose will draw behind them -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Edge-to-edge: content draws behind system bars -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
